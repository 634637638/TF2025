# 生产环境配置
VITE_NODE_ENV=production
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=15000
# 后端服务器地址（用于图片等静态资源访问）
# 当前前后端部署在同一台主机并由 Nginx 代理 /uploads/ 到后端时，留空即可走同源地址
# 如果未来前端和后端分离部署，再改成新的后端完整地址，例如 https://api.example.com
VITE_BACKEND_URL=
VITE_TENCENT_MAP_KEY=
