# InventoryView 拆分进度

更新时间：2026-09-06

## 目标与边界

`InventoryView.vue` 的拆分只调整代码归属，不改变库存路由、字段权限、库存查询、入库/编辑/删除 API、上架状态和移动端交互。库存数据加载、写操作和权限判断继续由父页面控制。

## 当前进度

| 区块 | 状态 | 位置 | 说明 |
| --- | --- | --- | --- |
| 统计卡片 | 已完成 | `page/InventoryStatsCards.vue` | 只接收统计卡片、可见性和加载状态；不创建权限或 API 边界 |
| 搜索筛选区 | 已完成 | `page/InventorySearchFilters.vue` | 继续复用全局 `UnifiedSearchPanel`；库存字段、字段权限结果、品牌型号联动和查询事件由父页面传入 |
| 库存表格 | 已完成 | `page/InventoryTable.vue` | 表格、动态列渲染、空状态和分页已抽离；列权限、手机端双击、详情/出库/编辑/删除及分页加载仍由父页面控制 |
| 编辑设备弹窗 | 已完成 | `page/InventoryEditDialog.vue` | 表单、字段权限显示、IMEI/SN 模式、上架开关和响应式布局已抽离；校验、品牌型号加载、保存 API 和权限仍由父页面控制 |
| 内存筛选选项 | 已完成 | `InventoryView.vue` | 按后端规范字段 `size` 解析内存规格，统一使用 `extractResponseData`，过滤空值并去重；修复筛选下拉显示“无数据” |
| 基础资料与品牌联动 | 已完成 | `useInventoryBaseOptions.ts` | 门店、供应商、品牌、型号、颜色、内存、操作员加载和品牌型号联动已抽离；缓存、权限接口、错误提示和查询回调由组合式函数统一维护 |
| 库存查询与统计数据流 | 已完成 | `useInventoryData.ts` | 库存列表、分页、筛选参数、统计、空状态提示、错误处理和搜索防抖已抽离；保留统一缓存和加载状态 |
| 页面编排与数据流程 | 已完成 | `InventoryView.vue` | 主页面保留路由、权限、详情/编辑/删除/出库/导出及入库弹窗生命周期；所有拆出区块通过 props/events 与页面编排连接 |

## 验证记录

- 初始主文件：5,636 行。
- 当前主文件：4,313 行；统计卡片、全局检索适配层、库存表格、编辑弹窗、基础资料加载和库存查询数据流已抽离。
- 库存页面及已抽出的四个组件、两个组合式函数严格 ESLint：通过。
- TypeScript 类型检查：通过。
- 字段、权限、UI、TAB、表格、加载和数据新鲜度审计：已重新执行并通过。
- 内存筛选回归：后端返回 `{ data: { memories: [...] } }` 时可正确展示 `size`，不再依赖不存在的 `name` 字段。
- 筛选统计回归（2026-09-04）：供应商、门店、入库员、品牌、型号、颜色、内存、机况、日期和关键词会同时传入库存列表与统计接口；筛选无结果时统计卡片显示真实 `0`，不再被前端清空为“暂无数据”；统计请求带有竞态保护，快速切换筛选不会回填旧结果。
- 编辑规格回归（2026-09-06）：库存和销售编辑统一将品牌、型号、颜色、内存名称解析为数据库 `*_id` 后提交；型号/规格变更不再沿用弹窗打开时的旧 ID，保存后强制绕过列表缓存读取最新数据；无法解析的名称会阻止提交并提示用户。
- 编辑规格契约补强（2026-09-06）：库存详情接口补齐 `model`、`color`、`memory` 名称；销售品牌切换同步清空旧 `brand_id/model_id`；品牌型号响应统一经过 `extractResponseData` 解析，避免下拉列表因响应包装差异为空。
- 生产构建：通过；Inventory 页面产物正常生成，路由懒加载和原有弹窗行为保持不变。
