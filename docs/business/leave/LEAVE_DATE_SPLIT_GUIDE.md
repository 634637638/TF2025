# 休假申请按日期拆分功能说明

## 功能描述

当用户申请的休假天数超过本月可用天数时，系统会：
1. **按日期自动拆分**：前N天为休假，后M天为请假
2. **分别创建记录**：每天创建一条独立的考勤记录
3. **清晰显示提示**：确认对话框中显示具体日期分配

## 使用场景示例

### 场景：本月只有1天可用，申请5-6号（2天）

#### 1. 用户操作
- 选择日期范围：**2026年1月5日 - 1月6日**
- 系统计算：2天
- 本月可用：1天

#### 2. 确认对话框显示
```
┌─────────────────────────────────────────┐
│ 休假天数提示                              │
├─────────────────────────────────────────┤
│ 本月可用休假天数：1天                    │
│ 申请天数：2天                            │
│ 将自动创建：                             │
│ - 休假 1 天（带薪，不扣工资）            │
│ - 请假 1 天（无薪，扣工资）              │
│                                         │
│ 具体日期分配：                           │
│ 1月5日:休假                              │
│ 1月6日:请假                              │
│                                         │
│         [取消]        [继续]             │
└─────────────────────────────────────────┘
```

#### 3. 数据库记录（2条独立记录）

**记录1 - 1月5日（休假）**
```json
{
  "id": 101,
  "employee_id": 1,
  "record_type": "monthly_leave",
  "record_date": "2026-01-05",
  "monthly_leave_days": 1,
  "status": "pending",
  "created_at": "2026-01-05 10:00:00"
}
```

**记录2 - 1月6日（请假）**
```json
{
  "id": 102,
  "employee_id": 1,
  "record_type": "leave",
  "record_date": "2026-01-06",
  "leave_type": "事假",
  "leave_days": 1,
  "leave_reason": "休假申请（超过休假天数，自动转为请假）",
  "status": "pending",
  "created_at": "2026-01-05 10:00:01"
}
```

#### 4. 列表显示效果

| 日期 | 类型 | 天数 | 状态 | 说明 |
|------|------|------|------|------|
| 2026-01-05 | 休假 | 1天 | 待审批 | 带薪休假 |
| 2026-01-06 | 请假 | 1天 | 待审批 | 事假（自动转换） |

## 更多示例

### 示例1：可用2天，申请5-7号（3天）

**确认对话框：**
```
本月可用休假天数：2天
申请天数：3天
将自动创建：
- 休假 2 天（带薪，不扣工资）
- 请假 1 天（无薪，扣工资）

具体日期分配：
1月5日:休假
1月6日:休假
1月7日:请假
```

**数据库记录：**
- 1月5日：休假1天
- 1月6日：休假1天
- 1月7日：请假1天

### 示例2：可用3天（上月累积1天），申请5-8号（4天）

**确认对话框：**
```
本月可用休假天数：3天
申请天数：4天
将自动创建：
- 休假 3 天（带薪，不扣工资）
- 请假 1 天（无薪，扣工资）

具体日期分配：
1月5日:休假
1月6日:休假
1月7日:休假
1月8日:请假
```

**数据库记录：**
- 1月5日：休假1天
- 1月6日：休假1天
- 1月7日：休假1天
- 1月8日：请假1天

## 代码实现要点

### 前端（AttendanceView.vue）

```typescript
// 1. 生成日期分配说明
const dates: string[] = []
for (let i = 0; i < requestedDays; i++) {
  const d = new Date(startDate)
  d.setDate(d.getDate() + i)
  const dateStr = `${d.getMonth() + 1}月${d.getDate()}日`
  if (i < monthlyLeaveDays) {
    dates.push(`${dateStr}:休假`)
  } else {
    dates.push(`${dateStr}:请假`)
  }
}

// 2. 按日期分别创建记录
for (let i = 0; i < monthlyLeaveDays; i++) {
  // 创建休假记录（前N天）
  await attendanceApi.createAttendanceRecord({
    record_type: 'monthly_leave',
    record_date: dateStr,
    monthly_leave_days: 1
  })
}

for (let i = monthlyLeaveDays; i < requestedDays; i++) {
  // 创建请假记录（后N天）
  await attendanceApi.createAttendanceRecord({
    record_type: 'leave',
    record_date: dateStr,
    leave_type: '事假',
    leave_days: 1
  })
}
```

## 优势

1. **清晰明了**：每天独立记录，便于查看和审批
2. **自动分配**：前N天优先为休假，超过部分为请假
3. **灵活调整**：如需修改，可以单独调整某一天的记录
4. **准确计算**：工资计算时可以准确区分带薪和无薪天数
5. **审批友好**：审批人可以看到每天的具体类型

## 注意事项

1. **日期顺序**：系统按照日期范围从前往后分配，前N天为休假
2. **独立记录**：每天都是独立的考勤记录，可以单独修改
3. **状态同步**：所有创建的记录初始状态都是"待审批"
4. **批量操作**：如需批量审批，可以在列表中多选操作
